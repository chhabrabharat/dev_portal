# Dev-server port. 3000 is crm_frontend's and 3001 is patient_portal's, so this portal takes 3002
# to let all three run side by side. Committed on purpose - it's a local convention, not a secret.
PORT=3002

# Points the dev server's requests at the local backend. package.json's "proxy" would cover this
# too, but naming it explicitly means the same command works if the proxy is ever removed.
REACT_APP_API_BASE_URL=http://localhost:8080/api
